ContainerGovernanceAgent
  Manages a container's agents to ensure friendly behavior.

ContainerHeartbeatAgent
  Manages a container's agents to ensure liveness.

ContainerOperationAgent
  Manages a container and the TexaiCoin agent within the container. Interacts with the human container operator.

NetworkOperationAgent
  Manages the network, the containers, and the TexaiCoin agents within the containers. Interacts with human operators.

TopLevelGovernanceAgent
  Manages the container governance agents to ensure friendly behavior.

TopLevelHeartbeatAgent
  Manages the container heartbeat agents to ensure liveness.

TopmostFriendshipAgent
  Coordinates the top level agents to ensure friendly behavior.

XAIBlockchainArchiveAgent
  Archives the TexaiCoin blockchain and pending transactions.

XAIClientGatewayAgent
  Provides a wallet and processor client gateway for the TexaiCoin network.

XAIContainerAuditAgent
  Provides a container audit agent for the TexaiCoin network.

XAIContainerCertificateAuthorityAgent
  Provides self-signed X.509 certificates for roles in a particular container in the TexaiCoin
      network.

XAIContainerEpisodicMemoryAgent
  Provides episodic memory for agents in a particular container in the TexaiCoin network.

XAIContainerLogControlAgent
  Coordinates Java logging for agents in the TexaiCoin network.

XAIContainerTorrentAgent
  Provides bittorrent file transfers for agents in a particular container in the TexaiCoin network.

XAIFinancialAccountingAndControlAgent
  Provides financial accounting and control for the TexaiCoin network.

XAIMintAgent
  Provides a mint the TexaiCoin network.

XAINetworkConfigurationAgent
  Provides network configuration the TexaiCoin network. Maintains the assignment of
      nomadic agents to particular containers.

XAINetworkEpisodicMemoryAgent
  Coordinates episodic memory for agents in the TexaiCoin network.

XAINetworkLogControlAgent
  Coordinates Java logging for agents in the TexaiCoin network.

XAINetworkOperationAgent
  Manages the TexaiCoin agent network. Interacts with human operators.

XAINetworkSeedAgent
  Provides a TexaiCoin network seed for wallet and processor clients. Maintains the mapping of client gateways
      and their respective IP addresses.

XAINetworkTorrentAgent
  Coordinates bittorrent file transfers for agents in the TexaiCoin network.

XAIOperationAgent
  Manages a bitcoind instance.

XAIPrimaryAuditAgent
  Provides the primary audit agent for the TexaiCoin network.

XAIRecoveryAgent
  Provides fault recovery for the TexaiCoin network.

XAIRewardAllocationAgent
  Allocates the rewards resulting from minting new blocks for the TexaiCoin network.

